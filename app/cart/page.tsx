import Placeholder from "@/components/Placeholder";

export const metadata = { title: "Cart" };

export default function CartPage() {
  return <Placeholder title="Cart" note="Cart + summary. Phase 3." />;
}
