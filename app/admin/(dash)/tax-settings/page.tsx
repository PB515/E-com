import Placeholder from "@/components/Placeholder";

export const metadata = { title: "Admin · Tax Settings", robots: { index: false, follow: false } };

export default function AdminTaxSettingsPage() {
  return (
    <Placeholder
      title="Tax Settings"
      note="GSTIN · registered state · default GST rate (admin-editable). Phase 4."
    />
  );
}
