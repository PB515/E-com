import Placeholder from "@/components/Placeholder";

export const metadata = { title: "Admin · Returns", robots: { index: false, follow: false } };

export default function AdminReturnsPage() {
  return (
    <Placeholder
      title="Returns"
      note="Review requests → approve → issue credit note (reverses GST). Phase 4."
    />
  );
}
