import Placeholder from "@/components/Placeholder";

export const metadata = { title: "Checkout" };

export default function CheckoutPage() {
  return (
    <Placeholder
      title="Checkout"
      note="Address → place-of-supply GST → Razorpay (test) / COD. Phase 4."
    />
  );
}
