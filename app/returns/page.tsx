import Placeholder from "@/components/Placeholder";

export const metadata = { title: "Returns" };

export default function ReturnsPage() {
  return (
    <Placeholder
      title="Returns"
      note="7-day damaged/wrong only → credit note. Policy + request form. Phase 2/4."
    />
  );
}
